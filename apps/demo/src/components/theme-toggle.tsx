// Re-export ThemePicker as ThemeToggle for backward compatibility.
// All existing layout references to <ThemeToggle /> will now render the full theme picker.
export { ThemePicker as ThemeToggle } from "./theme-picker";
