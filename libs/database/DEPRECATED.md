# DEPRECATED

This library contains TypeORM entity definitions from the original NestJS API.
The API has been rewritten in Python (FastAPI + SQLAlchemy), and no TypeScript
consumers import this library.

Kept for reference only. Do not add new consumers.

See `apps/api/app/models/` for the authoritative SQLAlchemy models.
